import React, {FC} from "react";
import {Text} from "../Themed";
import {StyleSheet} from "react-native";

const GameBoardGrid: FC = () => {
    return (
        <Text>GameBoardGrid</Text>
    )
};

const styles = StyleSheet.create({});

export default GameBoardGrid;
